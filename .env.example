# Server
JWT_SECRET=change-this-to-a-random-string
PORT=4530

# Database configuration is managed via data/db-config.json (set up in the web UI).
# The env vars below are only used as fallback when db-config.json does not exist.
# DB_TYPE=sqlite
# DB_SQLITE_PATH=./data/novel.db
# DB_MYSQL_HOST=localhost
# DB_MYSQL_PORT=3306
# DB_MYSQL_USER=root
# DB_MYSQL_PASSWORD=
# DB_MYSQL_DATABASE=ai_novel
