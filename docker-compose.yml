services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - JWT_SECRET=change-this-to-a-random-string
      - DB_TYPE=sqlite
      - DB_SQLITE_PATH=/app/data/novel.db
    restart: unless-stopped
